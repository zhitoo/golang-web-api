info:
  name: change-password
  type: http
  seq: 5

http:
  method: post
  url: "{{baseUrl}}users/change-password"
  headers:
    - name: UserId
      value: "1"
    - name: Browser
      value: bruno
  body:
    type: json
    data: |-
      {
        "old_password":"09227121074",
        "new_password":"09227121074"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
