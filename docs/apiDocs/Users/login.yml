info:
  name: login
  type: http
  seq: 8

http:
  method: post
  url: "{{baseUrl}}users/login"
  headers:
    - name: UserId
      value: "1"
    - name: Browser
      value: bruno
  body:
    type: json
    data: |-
      {
        "mobile":"09227121074",
        "otp":"950443"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
