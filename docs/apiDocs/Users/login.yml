info:
  name: login
  type: http
  seq: 8

http:
  method: post
  url: "{{baseUrl}}users/login"
  headers:
    - name: UserId
      value: "1"
    - name: Browser
      value: bruno
  body:
    type: json
    data: |-
      {
        "mobile":"09227121074",
        "otp":"09227121074",
        "password":"09227121074"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        let data = res.body;
        bru.setVar("access_token", data.result.access_token);
        bru.setVar("refresh_token", data.result.refresh_token);
        console.log(data.result.access_token)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
