info:
  name: me
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}users/me"
  headers:
    - name: UserId
      value: "1"
    - name: Browser
      value: bruno
  body:
    type: json
    data: |-
      {
        "mobile":"09227121074",
        "otp":"768150"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
