info:
  name: header-binder-2
  type: http
  seq: 3

http:
  method: post
  url: "{{baseUrl}}test/header-binder-2"
  headers:
    - name: UserId
      value: "1"
    - name: Browser
      value: bruno

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
