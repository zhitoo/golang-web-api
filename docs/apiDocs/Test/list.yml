info:
  name: store
  type: http
  seq: 2

http:
  method: post
  url: "{{baseUrl}}test"
  body:
    type: form-urlencoded
    data:
      - name: id
        value: "3"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​