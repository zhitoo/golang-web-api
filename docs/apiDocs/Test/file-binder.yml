info:
  name: file-binder
  type: http
  seq: 8

http:
  method: post
  url: "{{baseUrl}}test/file-binder"
  headers:
    - name: UserId
      value: "1"
    - name: Browser
      value: bruno
  body:
    type: multipart-form
    data:
      - name: file
        type: text
        value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
