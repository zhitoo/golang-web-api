info:
  name: verify-otp
  type: http
  seq: 2

http:
  method: post
  url: "{{baseUrl}}otp/verify"
  headers:
    - name: UserId
      value: "1"
    - name: Browser
      value: bruno
  body:
    type: multipart-form
    data:
      - name: file
        type: text
        value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
