info:
  name: verify-otp
  type: http
  seq: 2

http:
  method: post
  url: "{{baseUrl}}otp/verify"
  body:
    type: json
    data: |-
      {
        "mobile":"09227121074",
        "otp":"123456"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
